{
  "name": "chat-workspace",
  "private": true,
  "dependencies": {
    "@types/supertest": "^7.2.0",
    "prettier": "^3.8.1",
    "supertest": "^7.2.2",
    "vitest": "^4.1.0"
  },
  "scripts": {
    "test": "vitest --run",
    "prettier": "prettier --write ."
  }
}
